{
  "name": "bom-squad-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "watch": "tsc -w -p tsconfig.json",
    "test": "vitest run",
    "prebuild": "node ../scripts/gen-build-info.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2",
    "vitest": "^4.0.8"
  }
}
