# Copy to .env and fill in. .env is gitignored.
# Only needed for the authenticated tests; without both they are not registered and the rest run.
E2E_EMAIL=e2e@bomsquad.test
# The JSON of a Firebase service account key, on one line. It mints the viewer's sign-in token.
FIREBASE_SERVICE_ACCOUNT=
